# API base URL for lead submission (no trailing slash)
# Production:
VITE_API_URL=https://crm-kukcha.vercel.app

# Local dev (Vue on 5173, API on same host): leave empty or set to your backend URL
# VITE_API_URL=
# Or if backend runs on 3000:
# VITE_API_URL=http://localhost:3000
